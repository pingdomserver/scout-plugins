metadata:
  usename:
    label: Replica Connection Username
  application_name:
    label: Replication Application Name
  client_hostname:
    label: Client Hostname (if known)
  client_addr:
    label: Client IP Address
  backend_start:
    label: Backend Start Date/Time
  sync_priority:
    label: Sync Priority
  sync_state:
    label: Sync State (Sync/Async)
  total_lag: 
    label: Total Lag
    units: Bytes
    precision: 1
  time_since_last_write:
    label: Time Since Last Write (Replica Only)
    units: seconds
    precision: 1
